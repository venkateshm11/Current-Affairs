# Dependencies
node_modules

# Claude Code orchestration files (not shipped)
# Anchored to repo root with a leading slash so they don't also match src/context/, etc.
/context
/.claude

# Build output
dist
dist-ssr

# Environment — NEVER commit real values
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.local
.idea

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Test / coverage
coverage

# Firebase
.firebase
firebase-debug.log
